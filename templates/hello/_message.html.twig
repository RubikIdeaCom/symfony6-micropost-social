<div style="color: grey;">
    {% if date(current_message.created) < date("-1year") %}
        Older than 1 year
    {% else %}
        {{current_message.created|date('d/m/Y')}}
    {% endif %}
</div>